library;

import 'protocol_types/general_types.dart';

/// Globally available utilities for Archipelago connections.
abstract class ArchipelagoGlobal {
  static final NetworkVersion supportedVersion = const NetworkVersion(0, 6, 0);
}
