name: archipelago
description: An Archipelago client library written in Dart
version: 0.1.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  web_socket_channel: ^3.0.2
  json_annotation: ^4.9.0
  collection: ^1.19.1
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  lints: ^5.0.0
  mockito: ^5.4.5
  test: ^1.25.15
